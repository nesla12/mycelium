// Tailwind CSS v4 via PostCSS. Driven through PostCSS rather than the Vite
// plugin so the build is decoupled from the bundler's resolver internals.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
